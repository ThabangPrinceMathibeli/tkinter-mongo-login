A desktop login application built with Python's Tkinter GUI framework and MongoDB for user authentication and storage.
Features:
User registration with username and password.
Secure login authentication against MongoDB.
Clean Tkinter GUI interface.
MongoDB Atlas or local MongoDB support.
